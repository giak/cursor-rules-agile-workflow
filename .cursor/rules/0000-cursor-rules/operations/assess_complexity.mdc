---
description:
globs:
alwaysApply: false
---
# Operation: Assess Complexity (Version 1.0.0)
# Assesses the complexity of a rule request to inform workflow strategy.

↹ operation•assess_complexity [p=1] {
    id: "@op/0000-cursor-rules/assess_complexity",
    version: "1.0.0",
    description: "Assesses the complexity of a rule request to determine workflow strategy",
    
    OP_assess = {
        type: "complexity_evaluation",
        function: "evaluate_rule_request_complexity",
        cognitive_load: "medium"
    }
    
    input: {
        rule_request: {
            goal: "Primary objective of the rule",
            context: "Contextual information for the rule's application",
            description_detail: "Detailed textual description of the desired rule logic and behavior",
            constraints: ["Specific constraints or non-goals (optional)"],
            examples: ["Examples of input/output or behavior (optional)"]
        }
    },

    output: {
        assessment_data: {
            complexity_score: "enum(low, medium, high, very_high)",
            justification: "Textual explanation for the assigned score",
            confidence: "float(0.0 to 1.0)",
            potential_ambiguities: ["Detected areas needing clarification (optional)"]
        }
    },

    Ω_process = {
        # Stage 1: Prepare assessment context
        prepare: {
            action: "Ω•internal/prepare_complexity_prompt",
            input: {
                request: "input.rule_request",
                criteria_kb: "KB_spec"
            },
            output: "prompt_data"
        },
        
        # Stage 2: LLM assessment
        assess: {
            action: "LLM•call",
            input: "prompt_data",
            contract: "LLM_assessor",
            output: "raw_assessment"
        },
        
        # Stage 3: Format and validate
        format: {
            action: "Ω•internal/format_assessment_output",
            input: "raw_assessment",
            output: "assessment_data",
            validation: "Λ.validate_assessment_schema(assessment_data)"
        }
    },
    
    dependencies: {
        KB_spec: "@kb/0000-cursor-rules/specs/assess_complexity",
        LLM_assessor: "@llm_contract/0000-cursor-rules/complexity_assessor"
    },
    
    Ψ_trace = {
        input_received: "M.log_input(rule_request)",
        assessment_complete: "M.record_assessment(assessment_data, OP_assess.type)"
    }
}

Σ operation•assess_complexity
